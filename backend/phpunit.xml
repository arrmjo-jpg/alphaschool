<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            Both the env block below (force="true") and the server block
            are required together, not redundant with each other.
            docker-compose.yml's env_file (.env.docker) sets DB_CONNECTION,
            DB_DATABASE, CACHE_STORE, SESSION_DRIVER, QUEUE_CONNECTION, and
            MAIL_MAILER as real process level environment variables inside
            the app container, which PHP's CLI SAPI mirrors into BOTH
            $_ENV and $_SERVER at process start. PHPUnit's env force="true"
            only overrides putenv()/$_ENV (see PhpHandler::handleEnvVariables
            in vendor/phpunit/phpunit/src/TextUI/Configuration/PhpHandler.php)
            and never touches $_SERVER, while Laravel's dotenv repository
            (Illuminate\Support\Env::getRepository) reads $_SERVER as one of
            its adapters and was observed resolving to the $_SERVER value
            here, not the forced $_ENV one. The server block below is what
            actually fixes it: PHPUnit's <server> tag always overwrites
            $_SERVER unconditionally, no force attribute needed or
            available. Without it, the real MariaDB alphaschool database
            gets migrate:fresh'd by RefreshDatabase on every test run
            despite APP_ENV=testing and DB_CONNECTION=sqlite both being
            correctly set in $_ENV. See
            docs/developer/rca-2026-07-21-test-database-wipe.md for the
            full incident writeup and the live reproduction that found
            this two-superglobal split.
        -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false" force="true"/>
        <env name="NIGHTWATCH_ENABLED" value="false" force="true"/>
        <server name="APP_ENV" value="testing"/>
        <server name="APP_MAINTENANCE_DRIVER" value="file"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="BROADCAST_CONNECTION" value="null"/>
        <server name="CACHE_STORE" value="array"/>
        <server name="DB_CONNECTION" value="sqlite"/>
        <server name="DB_DATABASE" value=":memory:"/>
        <server name="DB_URL" value=""/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="PULSE_ENABLED" value="false"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
        <server name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
