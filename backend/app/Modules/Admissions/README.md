# Admissions

**Layer:** Domain

Applicant lifecycle: submission, review, testing, decision, fee calculation, payment, conversion to Student.

**Rule:** Domain modules never import a sibling Domain module directly — only via events or that module's public service contract. See `docs/DOMAIN_BLUEPRINT.md` §1, §2, §9.

Populated starting Phase 4 — empty as of Sprint 0.1 (placeholder only, no code yet).
