/** The reason_codes context for TeacherAssignment (§32.6) -- shared by the Timeline, Close, and Cancel pages. Matches TeacherAssignment::temporalReasonContext() exactly. */
export const TEACHER_REASON_CONTEXT = 'subject_teacher_assignment'
