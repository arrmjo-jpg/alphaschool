# Admin Platform Foundation (ADR-0015) -- points at the backend's
# versioned API. AuthController issues bearer tokens (Sanctum personal
# access tokens), never cookies, so no separate "stateful domain" is
# needed here.
VITE_API_URL=http://localhost:8000/api/v1
